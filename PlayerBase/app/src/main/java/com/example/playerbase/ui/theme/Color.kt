package com.example.playerbase.ui.theme

import androidx.compose.ui.graphics.Color

// PlayerBase brand colors — estratti dal logo ufficiale
// Blu navy (sfondo principale del logo)
val PlayerNavy = Color(0xFF0B1A33)
val PlayerNavyDark = Color(0xFF07101F)
val PlayerNavyLight = Color(0xFF1C3A63)

// Oro/arancio (la "P" e il titolo "PLAYERBASE")
val PlayerGold = Color(0xFFF5A623)
val PlayerGoldDark = Color(0xFFC9860E)

// Arancio pallone da basket
val BasketOrange = Color(0xFFE8622C)
val BasketOrangeDark = Color(0xFF8A3A0F)
val BasketOrangeLight = Color(0xFFFFA766)
val BasketParquet = Color(0xFFC98A3E)

// Verde campo da calcio
val FieldGreen = Color(0xFF2E7D32)
val FieldGreenDark = Color(0xFF0F3D14)
val FieldGreenLight = Color(0xFF6ABF69)
val FieldLine = Color(0xFFF4F6F8)

// Viola WNBA, per distinguere lo stemma femminile da quello NBA
val WnbaPurple = Color(0xFF7B1FA2)

// Bianco/nero pallone da calcio
val SoccerWhite = Color(0xFFF4F6F8)
val SoccerBlack = Color(0xFF1B1B1B)

// Superfici
val PlayerSurfaceLight = Color(0xFFFFFFFF)
val PlayerSurfaceDark = Color(0xFF11213D)
